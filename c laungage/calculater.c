#include<stdio.h>
int main()
{
	int x,y,a,b,c,d;
	printf("enter your values");
	scanf("%d",&x);
	scanf("%d",&y);
	a=x+y;
	b=x-y;
	c=x*y;
	d=x/y;
	 
	if(x+y)
	{
		printf("Addition is %d\n",a);
			
	if(x-y)
	{
		printf("Substractionis %d\n",b);
	}
    if(x*y)
	{
		printf("Multiplication is %d\n",c);
	}
	if(x/y)
	{
		printf("Divison is %d\n",d);
	}
}
}















